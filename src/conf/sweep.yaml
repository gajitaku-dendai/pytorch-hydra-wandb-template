method: "bayes"
metric:
  name: "valid_r2_mean"
  goal: "maximize"
parameters:
  model:
    parameters:
      # learning_rate:
      #   distribution: "log_uniform_values"
      #   min: 0.00001
      #   max: 0.1
      # l2_rate:
      #   distribution: "log_uniform_values"
      #   min: 0
      #   max: 0.01
      batch_size:
        values: [8, 16, 32, 64]
      use_noise:
        values: [True, False]